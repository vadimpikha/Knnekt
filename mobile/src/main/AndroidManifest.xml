<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="knnekt">

    <uses-permission android:name="android.permission.INTERNET" />



    <application
        android:name=".presentation.KnnektApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity
            android:name=".presentation.ui.start.StartActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.ui.main.MainActivity"
            android:windowSoftInputMode="stateHidden|adjustResize"
            android:label="@string/app_name">
        </activity>

        <meta-data android:name="com.connectycube.pushnotifications.TYPE" android:value="FCM" />
        <meta-data android:name="com.connectycube.pushnotifications.SENDER_ID" android:value="@string/gcm_defaultSenderId" />
        <meta-data android:name="com.connectycube.pushnotifications.ENVIRONMENT" android:value="DEVELOPMENT" />

        <service
            android:name="com.connectycube.pushnotifications.services.fcm.FcmPushListenerService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>